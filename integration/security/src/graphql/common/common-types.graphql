scalar JSON
scalar DateTime

interface Node {
    id: ID!
}

interface ErrorResult {
    errorCode: ErrorCode!
    message: String!
}

interface PaginatedList {
    items: [Node!]!
    totalItems: Int!
}

type DeletionResponse {
    result: DeletionResult!
    message: String
}